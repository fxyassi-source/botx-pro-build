name: BotX Pro V56.0.2 Legacy Edge Build

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the V56.0.3 Kronos release workflow for current builds."
