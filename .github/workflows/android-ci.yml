name: Android CI (Disabled Legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: echo "Legacy Android CI is disabled. Use .github/workflows/build-android.yml only."
