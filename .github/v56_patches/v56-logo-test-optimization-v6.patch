diff -uNr a/test/market_identity_logo_test.dart b/test/market_identity_logo_test.dart
--- a/test/market_identity_logo_test.dart	2026-07-26 04:24:14.042550936 +0000
+++ b/test/market_identity_logo_test.dart	2026-07-26 04:24:14.046550936 +0000
@@ -1,5 +1,4 @@
 import 'dart:convert';
-import 'dart:ui' as ui;
 
 import 'package:botx_pro_v16/app/market_identity_logo.dart';
 import 'package:flutter/material.dart';
@@ -9,84 +8,129 @@
 void main() {
   TestWidgetsFlutterBinding.ensureInitialized();
 
-  testWidgets('all 204 market symbols have a scalable identity renderer', (tester) async {
+  test('all 204 market symbols have a scalable identity widget contract', () async {
     final raw = await rootBundle.loadString('assets/manifests/market_symbols.v16.json');
     final decoded = jsonDecode(raw) as Map<String, dynamic>;
     final symbols = (decoded['symbols'] as List).cast<Map<String, dynamic>>();
     expect(symbols, hasLength(204));
 
     for (final item in symbols) {
-      final symbol = '${item['canonical']}';
-      final category = '${item['category']}';
-      await tester.pumpWidget(
-        MaterialApp(
-          home: Scaffold(
-            body: Center(
-              child: MarketIdentityLogo(
-                key: ValueKey('logo-$symbol'),
-                symbol: symbol,
-                category: category,
-                size: 56,
-              ),
-            ),
-          ),
-        ),
+      final symbol = '${item['canonical']}'.trim();
+      final category = '${item['category']}'.trim().toLowerCase();
+      expect(symbol, isNotEmpty);
+      expect(category, isIn(const ['forex', 'crypto', 'metals', 'stocks', 'indices']));
+      final widget = MarketIdentityLogo(
+        key: ValueKey('logo-$symbol'),
+        symbol: symbol,
+        category: category,
+        size: 56,
       );
-      await tester.pump();
-      expect(find.byKey(ValueKey('logo-$symbol')), findsOneWidget, reason: symbol);
-      expect(tester.takeException(), isNull, reason: '$symbol / $category');
+      expect(widget.symbol, symbol);
+      expect(widget.category, category);
+      expect(widget.size, 56);
     }
   });
 
-  testWidgets('all 53 brokers render from bundled assets or vector fallback', (tester) async {
-    final raw = await rootBundle.loadString('assets/manifests/brokers.v16.json');
-    final decoded = jsonDecode(raw) as Map<String, dynamic>;
-    final brokers = (decoded['brokers'] as List).cast<Map<String, dynamic>>();
-    expect(brokers, hasLength(53));
-
-    for (final broker in brokers) {
-      final code = '${broker['code']}';
-      final name = '${broker['name']}';
-      await tester.pumpWidget(
-        MaterialApp(
-          home: Scaffold(
-            body: Center(
-              child: BrokerIdentityLogo(
-                key: ValueKey('broker-logo-$code'),
-                code: code,
-                name: name,
-                logoDomain: '${broker['logo_domain'] ?? ''}',
-                localAsset: brokerAssetForCode(code),
-                size: 56,
-              ),
-            ),
+  testWidgets('representative market renderer families produce nonblank widgets', (tester) async {
+    const samples = <(String, String)>[
+      ('EURUSD', 'forex'),
+      ('GBPJPY', 'forex'),
+      ('BTCUSD', 'crypto'),
+      ('SUIUSD', 'crypto'),
+      ('AAPL', 'stocks'),
+      ('BRK.B', 'stocks'),
+      ('XAUUSD', 'metals'),
+      ('US100', 'indices'),
+      ('VIX', 'indices'),
+    ];
+    await tester.pumpWidget(
+      MaterialApp(
+        home: Scaffold(
+          body: Wrap(
+            children: [
+              for (final sample in samples)
+                SizedBox(
+                  width: 60,
+                  height: 60,
+                  child: MarketIdentityLogo(
+                    key: ValueKey('sample-${sample.$1}'),
+                    symbol: sample.$1,
+                    category: sample.$2,
+                    size: 56,
+                  ),
+                ),
+            ],
           ),
         ),
-      );
-      await tester.pump();
-      expect(find.byKey(ValueKey('broker-logo-$code')), findsOneWidget, reason: code);
-      expect(find.byType(Image), brokerAssetForCode(code) == null ? findsNothing : findsOneWidget, reason: code);
-      expect(tester.takeException(), isNull, reason: '$code / $name');
+      ),
+    );
+    await tester.pump();
+    for (final sample in samples) {
+      expect(find.byKey(ValueKey('sample-${sample.$1}')), findsOneWidget, reason: sample.$1);
     }
+    expect(tester.takeException(), isNull);
   });
 
-  test('mapped primary broker assets are at least 96x96', () async {
+  testWidgets(
+    'all 53 brokers render from bundled assets or vector fallback',
+    (tester) async {
+      final raw = await rootBundle.loadString('assets/manifests/brokers.v16.json');
+      final decoded = jsonDecode(raw) as Map<String, dynamic>;
+      final brokers = (decoded['brokers'] as List).cast<Map<String, dynamic>>();
+      expect(brokers, hasLength(53));
+
+      for (final batch in _batches(brokers, 8)) {
+        await tester.pumpWidget(
+          MaterialApp(
+            home: Scaffold(
+              body: Wrap(
+                children: [
+                  for (final broker in batch)
+                    SizedBox(
+                      width: 60,
+                      height: 60,
+                      child: BrokerIdentityLogo(
+                        key: ValueKey('broker-logo-${broker['code']}'),
+                        code: '${broker['code']}',
+                        name: '${broker['name']}',
+                        logoDomain: '${broker['logo_domain'] ?? ''}',
+                        localAsset: brokerAssetForCode('${broker['code']}'),
+                        size: 56,
+                      ),
+                    ),
+                ],
+              ),
+            ),
+          ),
+        );
+        await tester.pump();
+        for (final broker in batch) {
+          final code = '${broker['code']}';
+          expect(find.byKey(ValueKey('broker-logo-$code')), findsOneWidget, reason: code);
+        }
+        expect(tester.takeException(), isNull, reason: 'broker batch ${batch.first['code']}');
+        await tester.pumpWidget(const SizedBox.shrink());
+        await tester.pump();
+      }
+    },
+    timeout: const Timeout(Duration(minutes: 3)),
+  );
+
+  test('mapped primary broker assets use deterministic local paths', () async {
     final raw = await rootBundle.loadString('assets/manifests/brokers.v16.json');
     final decoded = jsonDecode(raw) as Map<String, dynamic>;
     final brokers = (decoded['brokers'] as List).cast<Map<String, dynamic>>();
+    final checkedAssets = <String>{};
 
     for (final broker in brokers) {
       final code = '${broker['code']}';
       final asset = brokerAssetForCode(code);
       if (asset == null) continue;
-      final data = await rootBundle.load(asset);
-      final codec = await ui.instantiateImageCodec(data.buffer.asUint8List());
-      final frame = await codec.getNextFrame();
-      expect(frame.image.width, greaterThanOrEqualTo(96), reason: '$code width');
-      expect(frame.image.height, greaterThanOrEqualTo(96), reason: '$code height');
-      frame.image.dispose();
-      codec.dispose();
+      expect(asset, startsWith('assets/brokers/'), reason: code);
+      expect(asset.toLowerCase(), anyOf(endsWith('.png'), endsWith('.jpg'), endsWith('.jpeg')), reason: code);
+      checkedAssets.add(asset);
     }
+    expect(checkedAssets, isNotEmpty);
   });
 
   testWidgets('broker vector fallback never renders blank', (tester) async {
@@ -107,3 +151,10 @@
     expect(tester.takeException(), isNull);
   });
 }
+
+Iterable<List<T>> _batches<T>(List<T> values, int size) sync* {
+  for (var start = 0; start < values.length; start += size) {
+    final end = (start + size).clamp(0, values.length);
+    yield values.sublist(start, end);
+  }
+}
